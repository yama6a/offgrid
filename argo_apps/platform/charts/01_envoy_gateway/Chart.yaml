apiVersion: v2
name: envoy-gateway
description: Envoy Gateway, the cluster's Gateway API data plane. It owns the Gateway API CRDs, the `eg` GatewayClass and the EnvoyProxy that pins the LoadBalancer IP.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: gateway-helm
    version: "1.9.1"
    repository: "oci://docker.io/envoyproxy"
