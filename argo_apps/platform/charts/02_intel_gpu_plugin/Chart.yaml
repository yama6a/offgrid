apiVersion: v2
name: intel-gpu-plugin
description: Reports the amd64 node's Intel iGPU as gpu.intel.com/i915, so the scheduler can place transcoding pods on it.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
