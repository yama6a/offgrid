apiVersion: v2
name: sealed-secrets
description: The Bitnami Sealed Secrets controller, so secrets can go into git as SealedSecret CRs.
# Its RSA private key exists only in the cluster. Back it up with lib/shell/03_backup_sealed_secrets_key.sh.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: sealed-secrets
    version: "2.20.0"
    repository: https://bitnami.github.io/sealed-secrets
