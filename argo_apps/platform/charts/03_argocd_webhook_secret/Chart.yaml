apiVersion: v2
name: argocd-webhook-secret
description: The sealed GitHub webhook secret, merged into webhook.github.secret of argocd-secret. 02b_argocd_webhook.sh writes it.
# Not in the wave 1 01_argocd chart. 02a_argocd.sh installs that chart before the wave 2 SealedSecret CRD exists,
# so a SealedSecret there aborts the first install.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
