{{- range .Values.classes }}
---
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ .name }}
value: {{ .value | int64 }}
globalDefault: false # true would raise every pod that sets no priorityClassName
description: {{ .description | quote }}
{{- end }}
