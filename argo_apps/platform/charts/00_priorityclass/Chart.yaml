apiVersion: v2
name: priorityclass
description: The two shared PriorityClasses, platform-critical and data-critical, that order eviction under memory pressure.
# Admission allows the built-in system-cluster-critical and system-node-critical only in kube-system.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
