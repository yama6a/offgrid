apiVersion: v2
name: grafana
description: Grafana, the dashboards UI and the only alerting engine. Its own login is off. The platform-ingress SSO is the only gate.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: grafana
    version: "10.5.15"
    repository: https://grafana.github.io/helm-charts
