apiVersion: v2
name: barman-cloud-plugin
description: The CloudNativePG Barman Cloud CNPG-I plugin. It archives WAL and takes base backups to S3. It must run in the operator's namespace.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
# Upstream ships a release manifest and no Helm chart. templates/plugin-barman-cloud.yaml is that manifest,
# copied unchanged. To bump it, follow this chart's README.md.
appVersion: "v0.13.0" # also the image tag in the vendored manifest
