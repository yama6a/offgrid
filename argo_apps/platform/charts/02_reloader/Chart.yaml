apiVersion: v2
name: reloader
description: Restarts a workload when a ConfigMap or Secret that it mounts changes. Kubernetes does not do this itself.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: reloader
    version: "2.2.17"
    repository: https://stakater.github.io/stakater-charts
