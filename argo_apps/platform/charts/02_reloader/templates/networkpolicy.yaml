# Default-deny for the reloader namespace. The controller can read every Secret in the cluster.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: reloader
  namespace: {{ .Release.Namespace }}
spec:
  endpointSelector: {}          # every pod in the reloader namespace
  ingress:
    # vmagent scrapes the controller.
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: monitoring
            app.kubernetes.io/name: vmagent
      toPorts:
        - ports:
            - { port: "9090", protocol: TCP }
  egress:
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - { port: "53", protocol: UDP }
            - { port: "53", protocol: TCP }
    # Watches ConfigMaps and Secrets and patches the workloads that mount them.
    - toEntities: [kube-apiserver]
