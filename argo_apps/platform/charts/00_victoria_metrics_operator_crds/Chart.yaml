apiVersion: v2
name: victoria-metrics-operator-crds
description: VictoriaMetrics operator CRDs only, with no controller. A separate chart lets the large CRDs sync with ServerSideApply.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: victoria-metrics-operator-crds
    version: "0.14.0" # must match 01_victoria_metrics_operator. Renovate groups the two, so bump both together.
    repository: https://victoriametrics.github.io/helm-charts/
