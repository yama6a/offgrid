apiVersion: v2
name: dead-node-watcher
description: Taints a node out-of-service after it is NotReady for long enough, so its volumes detach in seconds, not minutes.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
