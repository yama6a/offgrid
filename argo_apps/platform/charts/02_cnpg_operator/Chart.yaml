apiVersion: v2
name: cnpg-operator
description: The CloudNativePG operator and its CRDs. Each database is a workload that uses lib/helm/pg-cluster.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: cloudnative-pg
    version: "0.29.1"
    repository: https://cloudnative-pg.github.io/charts
