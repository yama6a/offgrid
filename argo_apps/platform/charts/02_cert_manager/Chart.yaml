apiVersion: v2
name: cert-manager
description: cert-manager issues and renews X.509 certificates. 03_gateway ships the ClusterIssuers.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: cert-manager
    version: "v1.21.2"
    repository: https://charts.jetstack.io
