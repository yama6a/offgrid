apiVersion: v2
name: platform-ingress
description: The platform ingress. A Gateway and route per host for the platform UIs and ntfy, from the shared ingress chart. Adds one hand-written route for the Argo CD webhook.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: ingress
    version: "*"
    repository: "file://../../../../lib/helm/ingress"
