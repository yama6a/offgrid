apiVersion: v2
name: vm-backup
description: One CronJob that exports the VMSingle and VLSingle stores over HTTP and streams each to S3 through gzip. lib/shell/10e_vm_backup.sh commits the sealed S3 credentials.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
