apiVersion: v2
name: redis-backup
description: One CronJob that finds every durable Redis in the cluster by label and dumps each to S3 as an RDB. lib/shell/10c_redis_backup.sh commits the sealed S3 credentials.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
