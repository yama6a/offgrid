apiVersion: v2
name: argocd
description: Argo CD, the GitOps engine. 02a_argocd.sh installs it once, then it manages itself from this chart.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: argo-cd
    version: "10.9.2"
    repository: https://argoproj.github.io/argo-helm
