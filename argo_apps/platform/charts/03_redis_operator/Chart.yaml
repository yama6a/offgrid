apiVersion: v2
name: redis-operator
description: The OpsTree Redis operator and its CRDs. Each workload renders its own instance with lib/helm/redis-instance.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: redis-operator
    version: "0.26.1"
    repository: https://ot-container-kit.github.io/helm-charts/
