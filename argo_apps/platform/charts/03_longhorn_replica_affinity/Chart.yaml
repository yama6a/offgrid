apiVersion: v2
name: longhorn-replica-affinity
description: Wraps the upstream chart and adds only the CiliumNetworkPolicy, which upstream does not ship.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: longhorn-replica-affinity
    version: "0.3.19"
    repository: "oci://ghcr.io/yama6a/charts"
