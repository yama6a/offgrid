apiVersion: v2
name: nic-keeper
description: Per-node DaemonSet that mitigates the Raspberry Pi 5 macb NIC wedge at runtime. The OS repo owns the agent.
type: application
version: 0.1.0 # local chart, never bump version

# Authored in the OS repo because it is hardware/OS-specific, published there on merge to main. Bump this
# version to adopt a new one; renovate opens that PR itself.
dependencies:
  - name: nic-keeper
    version: "1.0.0"
    repository: oci://ghcr.io/yama6a/charts
