apiVersion: v2
name: cilium
description: Cilium, the CNI, with LB-IPAM, L2 announcements and WireGuard encryption. 01_cilium.sh installs it, then Argo CD adopts it.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: cilium
    version: "1.20.2"
    repository: https://helm.cilium.io
