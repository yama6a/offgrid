apiVersion: v2
name: victoria-metrics-operator
description: The VictoriaMetrics operator. It reconciles the VM* and VL* CRs and converts ServiceMonitor, PodMonitor and PrometheusRule objects to their VM kinds.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: victoria-metrics-operator
    version: "0.67.3"
    repository: https://victoriametrics.github.io/helm-charts/
