apiVersion: v2
name: blackbox-exporter
description: HTTPS probes of every ingress host, sent from inside the cluster to the public name. Includes the VMProbe objects that vmagent runs them with.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: prometheus-blackbox-exporter
    version: "11.19.1"
    repository: https://prometheus-community.github.io/helm-charts
