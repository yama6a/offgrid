apiVersion: v2
name: rabbitmq
description: RabbitMQ, both operators and their CRDs, the shared broker and the vhost. One chart puts the CRDs and CRs in one sync.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: rabbitmq-cluster-operator
    version: "0.6.18"
    repository: "oci://ghcr.io/cloudpirates-io/helm-charts"
