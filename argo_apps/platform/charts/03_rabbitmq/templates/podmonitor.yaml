{{- /*
The queue alerts need /metrics/detailed. Each pod, not the Service, because only a queue's leader reports it.
*/ -}}
{{- if .Values.metrics.podMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rabbitmq-broker
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.name }}
    app.kubernetes.io/component: rabbitmq
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.name }}
      app.kubernetes.io/component: rabbitmq   # only the 3 broker pods, not the operators
  podMetricsEndpoints:
    - port: prometheus
      path: /metrics
      interval: 30s
    - port: prometheus
      path: /metrics/detailed
      interval: 30s
      params:
        family:
          - queue_coarse_metrics    # rabbitmq_detailed_queue_messages{,_ready,_unacked}, the depth per queue
          - queue_consumer_count    # rabbitmq_detailed_queue_consumers, the consumers per queue
{{- end }}
