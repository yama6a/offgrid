apiVersion: v2
name: victoria-logs
description: Log storage and collection. A single-replica VLSingle store, plus the victoria-logs-collector DaemonSet that tails every container log and the node system logs.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: victoria-logs-collector
    version: "0.3.7"
    repository: https://victoriametrics.github.io/helm-charts/
