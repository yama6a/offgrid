apiVersion: v2
name: google-sso
description: Central Google SSO. One SecurityPolicy per domain, with an email allowlist per host and one shared google-sso.<domain> callback host. To gate a host, add it to hosts in values.yaml.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: ingress
    version: "*"
    repository: "file://../../../../lib/helm/ingress"
