apiVersion: v2
name: platform-apps
description: Every platform Application. They render from templates/, so repoURL lives in one place.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
