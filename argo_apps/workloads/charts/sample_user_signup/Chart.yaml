apiVersion: v2
name: sample-user-signup
description: The sample-app /signup binary. It publishes create-user-command every 10s and consumes users.created and the audit fanout.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it

dependencies:
  - name: rabbitmq-topology
    version: "*"
    repository: "file://../../../../lib/helm/rabbitmq-topology"
