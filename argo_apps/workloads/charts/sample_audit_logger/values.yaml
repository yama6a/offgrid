app:
  name: sample-audit-logger
  image: "ghcr.io/yama6a/pi5-k8s-sample-app:23"   # one image with three binaries: /manager, /signup, /auditor
  command: ["/auditor"]
  replicas: 1
  resources:
    requests: { cpu: 10m, memory: 16Mi }
    limits:   { memory: 32Mi }

rabbitmq-topology:
  user: sample-audit-logger        # same as app.name
  subscribeEvents:
    - { exchange: user-audit-logger }   # a fanout ignores routingKey
