apiVersion: v2
name: workload-apps
description: Every workload Application. They render from templates/, so repoURL lives in one place.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
