# sample-audit-logger runs the sample-app /auditor binary. It only consumes messages from the user-audit-logger fanout.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sample-audit-logger
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=true # without it, topology CR fields the server defaults stay OutOfSync
spec:
  project: default
  revisionHistoryLimit: 0
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: HEAD
    path: argo_apps/workloads/charts/sample_audit_logger
  destination:
    server: https://kubernetes.default.svc
    namespace: sample-audit-logger
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: -1
      refresh: true
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
