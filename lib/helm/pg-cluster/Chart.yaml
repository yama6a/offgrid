apiVersion: v2
name: pg-cluster
description: Shared chart that renders the CloudNativePG resources for one Postgres instance.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
