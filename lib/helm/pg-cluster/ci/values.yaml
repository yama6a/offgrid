# CI fixture with every required value set.
name: ci-pg
postgresVersion: "18"
highAvailability: true
size: 10Gi
deletionProtection: true
resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    memory: 512Mi
allowedClients:
  - matchLabels: { app: ci }
