apiVersion: v2
name: redis-instance
description: Shared chart that renders one standalone OpsTree Redis CR, its ServiceMonitor and a default-deny CiliumNetworkPolicy. A consumer adds it once per instance under an alias, like pg-cluster.
type: application
version: 0.1.0 # nothing publishes this chart, so never bump it
