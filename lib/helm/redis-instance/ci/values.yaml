# CI fixture with every required value set.
name: ci-redis
redisVersion: "v8.10.1"
persistence: true
deletionProtection: true
initialFixedDiskSize: 1Gi
resources:
  requests: { cpu: 25m, memory: 64Mi }
  limits: { memory: 96Mi }
allowedClients:
  - matchLabels: { app: ci }
