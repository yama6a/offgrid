# CI fixture with one entry of each kind, so every template renders.
user: ci
publishEvents:
  - { name: ci-events, type: topic }
consumeCommands:
  - { name: ci-commands }
subscribeEvents:
  - { exchange: other-events }
sendCommands:
  - other-commands
