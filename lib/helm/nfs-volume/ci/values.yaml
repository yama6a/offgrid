# CI fixture, not a deployment. With the default empty volumes list the chart renders nothing, so lint and
# kubeconform would check nothing. This is the smallest set that renders.
volumes:
  - name: ci-media
    server: 192.168.10.220
    path: /mnt/pool/media
    capacity: 50Ti
