# CI fixture. The default empty list renders nothing, so lint would check nothing.
ingresses:
  - name: ci
    domain: example.com
    issuer: letsencrypt-prod
    hosts:
      - subdomain: app
        targetService: ci-app
        targetPort: 8080
      - subdomain: "@"
        redirectTo: app.example.com
