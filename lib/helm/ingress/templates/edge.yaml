{{- /* Renders the consumer's edge from its `ingresses` values, so a consumer needs no template of its own.
       The guard renders nothing for a consumer that uses only the named templates. google-sso is one: it
       calls ingress.renderIngress itself. */ -}}
{{- if .Values.ingresses }}
{{ include "ingress.render" . }}
{{- end }}
